# Forms helpers

Shared form utilities for the web experience will live here. The legacy
`services/ui` location has been retired in favor of the `apps/web`
workspace path used by the new Docker builds.
